namespace AISwarm.Infrastructure.Eventing;

/// <summary>
/// Base interface for all event payloads in the system
/// </summary>
public interface IEventPayload
{
    // Common properties that all event payloads should have can be added here
    // For now, this serves as a marker interface for type safety
}